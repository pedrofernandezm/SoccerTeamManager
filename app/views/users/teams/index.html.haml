%h2 Teams
%table.table.table-hover.table-striped
  %thead
    %tr
      %th Name
      %th Players
      %th Owner
  %tbody
    - @teams.each do |team|
      %tr
        %td= team.name
        %td 0
        %td= team.user.full_name
